export interface User {
    id: string;
    firstName: string;
    lastName: string;
    userName: string;
    password: string;
}

export interface UserNameAndPassword {
    userName: string;
    password: string;
}